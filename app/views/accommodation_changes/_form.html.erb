<%= form_for(@accommodation_change) do |f| %>
  <% if @accommodation_change.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@accommodation_change.errors.count, "error") %> prohibited this accommodation_change from being saved:</h2>

      <ul>
      <% @accommodation_change.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :field %><br />
    <%= f.text_field :field %>
  </div>
  <div class="field">
    <%= f.label :old_value %><br />
    <%= f.text_area :old_value %>
  </div>
  <div class="field">
    <%= f.label :new_value %><br />
    <%= f.text_area :new_value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
