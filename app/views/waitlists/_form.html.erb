<%= form_for(@waitlist) do |f| %>
  <% if @waitlist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@waitlist.errors.count, "error") %> prohibited this waitlist from being saved:</h2>

      <ul>
      <% @waitlist.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :accommodation_id %><br />
    <%= f.number_field :accommodation_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
