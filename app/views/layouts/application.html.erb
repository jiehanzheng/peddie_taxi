<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "PeddieTaxi" %></title>
    <%= csrf_meta_tags %>

    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <%= favicon_link_tag 'images/favicon.ico', :rel => 'shortcut icon' %>
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <%= link_to "Peddie Taxi", root_path, :class => "brand" %>
          <div class="nav-collapse collapse">
            <%= tabs_tag(:builder => ApplicationHelper::BootstrapNavTabBuilder) do |tab| %>
              <%= tab.venues   'Venues', venues_path %>
              <%= tab.requests 'Requests', requests_path %>
              <%= tab.rides    'Rides', accommodations_path %>
            <% end %>
          </div>
        </div>
      </div>
    </div>

    <%= yield :stuff_outside_of_container %>

    <div class="<%= content_for?(:outer_div_class) ? content_for(:outer_div_class) : 'container' %>">
      <div class="row">
        <%= bootstrap_flash %>
      </div>

      <%= yield %>

    </div>

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>
    <%= yield :scripts %>

  </body>
</html>
