<div class="hero-unit">
  <p><%= proposal_status_badge(@proposal) %></p>
  <h1>Trip to <%= @proposal.venue.name %></h1>
  <p><%= @proposal.venue.vicinity %></p>
  <p>
    <%= link_to "Sign up", { :controller => "signups",
                             :action => "new",
                             :proposal_id => @proposal.id },
                           { :class => "btn btn-primary btn-large" } %>
    <%= link_to "View venue", @proposal.venue,
                              { :class => "btn btn-large" } %>
    <%#= link_to "Edit", edit_venue_path, { :class => "btn btn-large" } %>
    <%#= link_to "Delete", {}, { :confirm => "Are you sure?", 
                                :method => :delete, 
                                :class => "btn btn-danger btn-large" } %>
  </p>
</div>

<div class="row">
  <div class="span3">
    <div class="page-header">
      <h2>Roaster</h2>
    </div>

  </div>
  <div class="span9">
    <div class="page-header">
      <h2>Trip information</h2>
    </div>
    <table class="table table-bordered table-hover map-data-source">
      <tbody>
        <tr>
          <th scope="row">Leaving at</th>
          <td>
            <%= @proposal.leaving_at.to_s(:peddie_dumb) %>
            <span class="muted">
              (<%= the_time.strftime("%A") %>, <%= distance_of_time_in_words(Time.now, @proposal.leaving_at) %> <%= from_now_or_ago(@proposal.leaving_at) %>)
            </span>
          </td>
        </tr>
        <tr>
          <th scope="row">Returning at</th>
          <td>
            <%= @proposal.returning_at.to_s(:peddie_dumb) %>
          </td>
        </tr>
        <tr>
          <th scope="row">Proposed by</th>
          <td><%= @proposal.user.name_and_graduation_year %></td>
        </tr>
      </tbody>
    </table>
  </div>
</div>
